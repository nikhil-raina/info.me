/**
 * @provideModule WorkHistoryStyle
 * @flow
 */

import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  MainContainer: {
    flex: 1,
    backgroundColor: '#F2E6C2',
    alignItems: 'center',
    justifyContent: 'center',
  },
});
